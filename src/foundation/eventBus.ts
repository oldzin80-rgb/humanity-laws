export interface DomainEvent<T = Record<string, unknown>> { eventId: string; type: string; occurredAt: string; payload: T }
export type EventHandler<T = Record<string, unknown>> = (event: DomainEvent<T>) => void | Promise<void>;
export class EventBus { private readonly events: DomainEvent[] = []; private readonly handlers = new Map<string, EventHandler[]>(); on<T>(type: string, handler: EventHandler<T>) { this.handlers.set(type, [...(this.handlers.get(type) ?? []), handler as EventHandler]); } async publish<T>(type: string, payload: T): Promise<DomainEvent<T>> { const event = { eventId: `event_${Date.now()}`, type, occurredAt: new Date().toISOString(), payload }; this.events.push(event as DomainEvent); for (const handler of this.handlers.get(type) ?? []) await handler(event as DomainEvent); return event; } all(): DomainEvent[] { return [...this.events]; } }
