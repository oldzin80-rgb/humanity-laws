export interface TelemetryMetric { metricId: string; name: string; value: number; unit: string; recordedAt: string; tags?: Record<string, string> }
export class Telemetry { private readonly metrics: TelemetryMetric[] = []; record(params: { name: string; value: number; unit: string; tags?: Record<string, string> }): TelemetryMetric { const metric = { metricId: `metric_${Date.now()}`, name: params.name, value: params.value, unit: params.unit, recordedAt: new Date().toISOString(), ...(params.tags ? { tags: params.tags } : {}) }; this.metrics.push(metric); return metric; } all(): TelemetryMetric[] { return [...this.metrics]; } }
