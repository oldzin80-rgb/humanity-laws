import type { MemberProfile, MembershipStatus } from "./types.js";
export interface MemberRepository { findById(memberId: string): Promise<MemberProfile | null>; findByEmail(email: string): Promise<MemberProfile | null>; save(member: MemberProfile): Promise<MemberProfile> }
export class InMemoryMemberRepository implements MemberRepository { private readonly members = new Map<string, MemberProfile>(); async findById(id: string) { return this.members.get(id) ?? null; } async findByEmail(email: string) { return [...this.members.values()].find((m) => m.email.toLowerCase() === email.toLowerCase()) ?? null; } async save(member: MemberProfile) { const saved = { ...member, updatedAt: new Date().toISOString() }; this.members.set(member.memberId, saved); return saved; } }
export function createMemberProfile(params: { memberId: string; email: string; displayName: string; membershipStatus?: MembershipStatus }): MemberProfile { const now = new Date().toISOString(); return { memberId: params.memberId, email: params.email, displayName: params.displayName, roles: ["MEMBER"], membershipStatus: params.membershipStatus ?? "FREE", createdAt: now, updatedAt: now }; }
export async function activateMember(repo: MemberRepository, memberId: string): Promise<MemberProfile> { const member = await repo.findById(memberId); if (!member) throw new Error("Member not found"); return repo.save({ ...member, membershipStatus: "ACTIVE" }); }
